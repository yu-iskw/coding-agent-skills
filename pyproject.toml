[project]
name = "coding-agent-skills"
version = "0.2.0"
description = "Verified, portable engineering skills for agent-ready repositories."
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
dependencies = ["skills-ref"]

[tool.uv.sources]
skills-ref = { git = "https://github.com/agentskills/agentskills.git", subdirectory = "skills-ref" }
